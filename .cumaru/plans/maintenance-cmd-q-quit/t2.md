---
human_revised: false
plan: maintenance-cmd-q-quit
summary: Make Cmd+Q quit cleanly — explicit onmenuclick handler, or document the runtime gap if none can terminate.
task: T2
depends-on: [T1]
concerns: [specs/runtime]
files: [src/main.ts]
status: pending
apps: [host]
aux: []
---

# T2 — Fix or attribute

## What to do

Based on T1's attribution, make Cmd+Q terminate the process cleanly — most likely by replacing
the unproven `{ role: "quit" }` with an explicit item handled through `onmenuclick` calling
`win.close()` — or record the blocker in `specs/runtime`.

## Context

The runtime exposes `onmenuclick` on the window (verified API surface) but nothing documents
which roles it implements. An explicit handler is testable and does not depend on undocumented
role mapping.

## Implementation

1. Apply the minimal change implied by T1's attribution.
2. Static checks: `deno check src/main.ts`, `deno fmt --check`, `deno test --allow-read tests/`.
3. Live validation: relaunch, press Cmd+Q, confirm the process is gone.

## Done when

- [ ] Cmd+Q terminates the process cleanly, validated live — or the gap is documented in specs.
